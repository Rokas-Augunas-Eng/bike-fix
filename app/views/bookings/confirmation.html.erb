<div>
  <h1>Booking Confirmation</h1>
  <h2>My Basket</h2>
  <p>Service wil be in XX minutes</p>
</div>

<div class="order_summary">
  <h2>Items</h2>
  <%= @booking.services.each do |service| %>
    <ul class="flex">
      <li><%= service.repair_name %></li>
      <li><%= service.cost %></li>
    </ul>
  <% end %>
</div>
<%= @booking.user.email %>

<div id="map"
  style="width:100%; height: 400px;"
  data-markers="<%= @markers.to_json %>"
  data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
</div>

<div class="flex">
  <h2>Total</h2>
  <p>"£Insert Total"</p>
</div>
